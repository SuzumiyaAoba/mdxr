import { createReport } from "./report-view.js";

export const Evidence = createReport("Evidence");
export const Provenance = createReport("Provenance");
export const Assumptions = createReport("Assumptions");
export const Limitations = createReport("Limitations");
export const DocumentHistory = createReport("DocumentHistory");
